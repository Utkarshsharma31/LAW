import React, { useState } from 'react'

function test() {
    const [showText, setShowText] = useState("Hello *im *trying to do i")
    const text="*Hello *im *trying to do i"
    const reformatText=(showText)=>{
        // console.log(tex)
        const splitit=text.split("*")


    }
    return (
        <div>
            
        </div>
    )
}

export default test
